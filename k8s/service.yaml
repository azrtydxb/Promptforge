apiVersion: v1
kind: Service
metadata:
  name: promptforge-service
  namespace: promptforge
  labels:
    app: promptforge
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: promptforge
