# Dependencies
node_modules
.pnp
.pnp.js

# Build outputs
.next
dist
build
out

# Git
.git
.gitignore
.gitattributes

# Documentation
*.md
README*
CHANGELOG*
LICENSE*
docs/

# Environment files (except production)
.env*
!.env.production

# Testing
coverage
.nyc_output
tests/
test/
__tests__/
*.test.js
*.test.ts
*.spec.js
*.spec.ts
jest.config.*
cypress/

# Development
.vscode
.idea
*.swp
*.swo
*~
.DS_Store

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
*.log

# Kubernetes
k8s/
kubernetes/
helm/

# CI/CD
.github/
.gitlab-ci.yml
.travis.yml
.circleci/

# Docker
Dockerfile*
docker-compose*.yml
.dockerignore

# Scripts (if not needed at runtime)
scripts/

# Prisma migrations (only keep schema in final image).
# seed.ts is kept in the build context so the :migrate image (builder stage) can run it;
# the distroless runtime image copies only schema.prisma, so seed.ts is never shipped.
prisma/migrations/
