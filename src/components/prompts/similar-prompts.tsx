"use client";

interface SimilarPromptsProps {
  promptId: string;
  className?: string;
}

export function SimilarPrompts({ promptId: _promptId, className: _className }: SimilarPromptsProps) {
  // Similar prompts feature removed (AI functionality)
  return null;
}
